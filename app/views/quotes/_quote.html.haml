.quote 
	= link_to quote.name, quote_path(quote)
	.quote__actions 
		= button_to 'Delete', quote_path(quote), method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn--light"
		= link_to 'Edit', edit_quote_path(quote), class: "btn btn--light"